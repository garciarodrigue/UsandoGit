trabajando en git

#╰─ git config --global user.name "Tenshi Kira"

#╰─ git config --global user.email garciarodrigueza587@gmail.com

#╰─ git init

#╰─ git config --global core.autocrlf input

#╰─ git status

#╰─ git add archivo.txt 

#╰─ git commit -m "que paso??"

#╰─ git rm archivo.txt //esto se elimina en stage

#╰─ git mv archivo.txt nuevo.txt //renombra en stage

#╰─ .gitignore /ignorara lo que le digamos

#╰─ git status -s //muestra menos detalles

#╰─ git diff //muetra las lineas modific se sale con "q"

#╰─ git diff --staged //vemos modif en staged

#╰─ git log --oneline // vemos info de todas las mod

#╰─ git branch //vemos en que rama trabajamos

#╰─ git checkout -b nombre_rama //creamos rama

#╰─ git checkout nombreRama //asi nos movemos entre ramas

#╰─ git merge nombreRama // asi llevamos mod a la rama donde no estan mod

#╰─ git remote add origin https://github.com/garciarodrigue/Repo.git

╰─ git push -u origin master //master o main o nombreRama
